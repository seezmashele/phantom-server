# HTTP Server Configuration
# This file contains example environment variables for configuring the HTTP server.
# Copy this file to .env and modify the values as needed.

# Server Configuration
# The port on which the HTTP server will listen
PORT=8080

# Path to JSON configuration file (optional)
# If specified, the server will load additional configuration from this JSON file
# Environment variables will override JSON configuration values
CONFIG_PATH=config.json

# Timeout Configuration
# Graceful shutdown timeout in seconds
SHUTDOWN_TIMEOUT=30

# HTTP request read timeout in seconds
READ_TIMEOUT=10

# HTTP response write timeout in seconds
WRITE_TIMEOUT=10

# CORS Configuration
# Comma-separated list of allowed origins for CORS
# Use "*" to allow all origins (not recommended for production)
ALLOWED_ORIGINS=*

# Comma-separated list of allowed HTTP methods
ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS

# Logging Configuration
# Enable or disable request logging (true/false)
ENABLE_LOGGING=true

# Example Production Configuration:
# PORT=3000
# SHUTDOWN_TIMEOUT=45
# READ_TIMEOUT=15
# WRITE_TIMEOUT=15
# ALLOWED_ORIGINS=https://myapp.com,https://www.myapp.com
# ALLOWED_METHODS=GET,POST,PUT,DELETE
# ENABLE_LOGGING=true

# Example Development Configuration:
# PORT=8080
# SHUTDOWN_TIMEOUT=10
# ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
# ENABLE_LOGGING=true